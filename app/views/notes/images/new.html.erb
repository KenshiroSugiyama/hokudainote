<div class="row">
<div class="col-sm-4">
    <%= form_with(model: @images ) do |f| %>
    <div class = 'images'>
        <%= f.label "画像"%>
        <%= f.file_field :image %>
        <%= f.submit '画像をアップロード', class: 'btn btn-primary btn-block'%>
    </div>
    <% end %>
</div>
</div>